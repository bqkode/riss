import { RissCanvas } from '../canvas';
import { LayoutBox } from '../../types/layout';
import { RenderContext } from '../../types/render';

export function draw(_canvas: RissCanvas, _box: LayoutBox, _ctx: RenderContext): void {
  // Spacer is invisible - it only takes up layout space
}
